{
  "name": "volt-drive-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:alanquantic/voltdrive.git"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "build": "esbuild src/app.jsx --bundle --outfile=public/bundle.js --loader:.jsx=jsx",
    "start": "node server.js",
    "dev": "npm run build && node server.js"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^4.19.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
